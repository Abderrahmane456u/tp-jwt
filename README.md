# 🛡️ TP – Authentification JWT (Backend Flask + Frontend HTML/JS)

Ce projet est un TP complet d’authentification avec **JWT**, réalisé en Python (Flask) pour le backend et en HTML/JavaScript pour le frontend.

Il inclut :

✔ Login + Access Token  
✔ Refresh Token  
✔ Hashage des mots de passe (bcrypt)  
✔ JWT enrichi (email + permissions)  
✔ Routes protégées par token  
✔ Routes publiques  
✔ Gestion des rôles (admin)  
✔ Gestion des permissions (write)  
✔ Gestion automatique des erreurs 401 côté frontend  

---

## 📁 Architecture du projet
atelier-jwt/
├── backend/
│ ├── app.py
│ ├── requirements.txt
│ └── .env
└── frontend/
└── index.html
